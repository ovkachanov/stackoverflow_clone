h3 Title:
= @question.title

h3 Body:
= @question.body
br
= link_to 'Delete question', question_path, method: :delete if user_signed_in?

= render 'answers/answer'

- if current_user
  = link_to 'Add answer', new_question_answer_path(question_id: @question)
br
  = link_to 'Back', root_path
