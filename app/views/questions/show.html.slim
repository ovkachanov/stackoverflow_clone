h3 Title
= @question.title

h3 Body
= @question.body
br
= link_to 'Delete question', question_path, method: :delete if user_signed_in?
br
h3 Answer's
.answers
  = render 'answers/answers'
br
- if user_signed_in?
  = render 'answers/form'

